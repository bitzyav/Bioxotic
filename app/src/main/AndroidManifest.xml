<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aviles.itzel.bioxotic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bioxotic">
        <activity android:name=".ui.calculate_size.CalculateSizeFragment"></activity>
        <activity android:name=".ui.species.SpeciesFragment" />
        <activity android:name=".SignUp" />
        <activity android:name=".request_code" />
        <activity android:name=".RequestEmail" />
        <activity
            android:name=".ForgotPassword"
            android:label="@string/title_activity_forgot_password"
            android:theme="@style/Theme.Bioxotic.NoActionBar" />
        <activity android:name=".SignIn" />
        <activity android:name=".SplashScreen" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>